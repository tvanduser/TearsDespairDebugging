package hw4.player;

/**
 * Enumeration representing the possible directions a player can move in the maze.
 */
public enum Movement {
    /** Move up in the maze. */
    UP,

    /** Move left in the maze. */
    LEFT,

    /** Move down in the maze. */
    DOWN,

    /** Move right in the maze. */
    RIGHT
}
